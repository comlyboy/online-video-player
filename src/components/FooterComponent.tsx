export function FooterComponent() {
  return (
    <footer className="border-t border-border-default">
      <div className="mx-auto flex max-w-7xl flex-col items-center justify-between gap-3 px-6 py-8 text-sm text-foreground-muted sm:flex-row">
        <p>Online Video Player. Built for streaming from direct video links.</p>
        <p>Third party platforms open on their own websites and are not affiliated with this app.</p>
      </div>
    </footer>
  );
}
